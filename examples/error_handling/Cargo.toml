[package]
name = "error_handling"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
regex = "1.10"
telers = { path = "../../telers", features = ["default-signal"] }
thiserror = "2.0"
tokio = { version = "1.36", features = ["macros"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
